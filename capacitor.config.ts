import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dwaitanidhi.app',
  appName: 'Dwaita Nidhi',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
